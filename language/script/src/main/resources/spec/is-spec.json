{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "http://pcsoft.org/intellij/plugin/inno-setup/isi-spec",
  "description": "Specification of all Inno Setup script sections, their attributes and flags used by the plugin for completion, validation and documentation.",
  "type": "object",
  "required": [
    "sections"
  ],
  "properties": {
    "sections": {
      "description": "List of all script sections (e.g. [Setup], [Files], [Code]) supported by the plugin.",
      "type": "array",
      "items": {
        "$ref": "#/definitions/section"
      }
    }
  },
  "definitions": {
    "fileTargets": {
      "description": "Set of file types a rule applies to: \"iss\" (script) and/or \"isl\" (language file).",
      "type": "array",
      "uniqueItems": true,
      "items": {
        "type": "string",
        "enum": [
          "iss",
          "isl"
        ]
      }
    },
    "exceptions": {
      "description": "Cases in which the constraint of the enclosing block does not apply. Every listed case inverts that constraint on its own; the block is written directly inside the constraint it lifts (\"mustExists\" or \"required\").",
      "type": "object",
      "properties": {
        "by-flag": {
          "description": "Flags of the same section entry that lift the constraint. Under \"mustExists\" the path is then only checked for invalid path characters (e.g. [Files] \"Source\" with \"external\"); under \"required\" the attribute may then be omitted, for every file type listed in \"files\" (e.g. [Files] \"DestDir\" with \"dontcopy\").",
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string"
          }
        },
        "by-attribute": {
          "description": "Names of other attributes of the same section that lift the constraint by taking its place. Only meaningful under \"required\": Inno Setup accepts either/or pairs (e.g. \"AppVersion\" may be replaced by \"AppVerName\"), so the attribute is not reported as missing while one of these is present.",
          "type": "array",
          "uniqueItems": true,
          "items": {
            "type": "string"
          }
        }
      },
      "additionalProperties": false
    },
    "mustExists": {
      "description": "Marks a path value as having to exist on the build machine at compile time. Presence of the key alone enables the check; an empty value (\"mustExists:\") is the normal form. Absence of the key means the value is a target/runtime path that is only checked for invalid path characters.",
      "type": [
        "object",
        "null"
      ],
      "properties": {
        "except": {
          "description": "Cases in which the existence check is skipped, leaving only the invalid-character check.",
          "$ref": "#/definitions/exceptions"
        }
      },
      "additionalProperties": false
    },
    "requiredSpec": {
      "description": "Marks an attribute as mandatory within its section. Absence of the key means the attribute is optional.",
      "type": "object",
      "required": [
        "files"
      ],
      "properties": {
        "files": {
          "description": "File types in which this attribute must be specified within its section (e.g. [\"iss\"], [\"isl\"]).",
          "$ref": "#/definitions/fileTargets"
        },
        "except": {
          "description": "Cases in which the attribute may be omitted although it is listed as mandatory.",
          "$ref": "#/definitions/exceptions"
        }
      },
      "additionalProperties": false
    },
    "section": {
      "description": "A single Inno Setup script section, identified by its header name and the kind of content it accepts.",
      "type": "object",
      "required": [
        "name",
        "type",
        "deprecated",
        "description",
        "attributes"
      ],
      "properties": {
        "name": {
          "description": "Section header name as written in the script without brackets (e.g. \"Setup\", \"Files\").",
          "type": "string"
        },
        "type": {
          "description": "Structural kind of the section: \"directive\" for Key=Value sections, \"parameter\" for Key: Value parameter lists, or \"code\" for the Pascal [Code] section.",
          "type": "string",
          "enum": [
            "directive",
            "parameter",
            "code"
          ]
        },
        "deprecated": {
          "description": "File types in which this section is deprecated (e.g. [\"iss\", \"isl\"]). Empty/absent = not deprecated.",
          "$ref": "#/definitions/fileTargets"
        },
        "required": {
          "description": "File types in which this section must be present (e.g. [\"iss\"] for scripts, [\"isl\"] for language files). Empty/absent = optional.",
          "$ref": "#/definitions/fileTargets"
        },
        "description": {
          "description": "Human-readable explanation of the section's purpose, shown in documentation and tooltips.",
          "type": "string"
        },
        "attributes": {
          "description": "List of keys or parameters that may appear inside this section.",
          "type": "array",
          "items": {
            "$ref": "#/definitions/attribute"
          }
        },
        "internationalization": {
          "description": "Whether keys in this section may carry a language prefix (e.g. \"en.MessageName\"); enables language-prefix completion. Applies to [Messages] and [CustomMessages]. Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "languageFile": {
          "description": "Whether this section is permitted in Inno Setup language files (.isl). Defaults to false.",
          "type": "boolean",
          "default": false
        },
        "since": {
          "description": "Inno Setup version in which this section was introduced (e.g. \"6.0.0\").",
          "type": "string"
        },
        "until": {
          "description": "Inno Setup version up to which this section is supported; absent if still current.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "attribute": {
      "description": "A single key (directive section) or parameter (parameter section) that can be set within a section.",
      "type": "object",
      "required": [
        "name",
        "type",
        "deprecated",
        "description",
        "array"
      ],
      "properties": {
        "name": {
          "description": "Name of the key or parameter as written in the script (e.g. \"AppName\", \"Source\").",
          "type": "string"
        },
        "type": {
          "description": "Value type of the attribute, defining how its value is parsed and validated.",
          "$ref": "#/definitions/attributeType"
        },
        "required": {
          "description": "Marks the attribute as mandatory. Absent = optional.",
          "$ref": "#/definitions/requiredSpec"
        },
        "deprecated": {
          "description": "File types in which this attribute is deprecated (e.g. [\"iss\", \"isl\"]). Empty = not deprecated.",
          "$ref": "#/definitions/fileTargets"
        },
        "description": {
          "description": "Human-readable explanation of the attribute's purpose, shown in documentation and tooltips.",
          "type": "string"
        },
        "array": {
          "description": "Whether the attribute accepts multiple values, e.g. a space- or comma-separated list of flags.",
          "type": "boolean"
        },
        "since": {
          "description": "Inno Setup version in which this attribute was introduced (e.g. \"6.0.0\").",
          "type": "string"
        },
        "until": {
          "description": "Inno Setup version up to which this attribute is supported; absent if still current.",
          "type": "string"
        }
      },
      "additionalProperties": false
    },
    "attributeType": {
      "description": "Discriminated union describing how an attribute's value is typed: a native primitive, a reference to another section, or a set of flags.",
      "oneOf": [
        {
          "description": "A primitive value such as a string, boolean or integer.",
          "type": "object",
          "required": [
            "kind",
            "dataType"
          ],
          "properties": {
            "kind": {
              "description": "Discriminator selecting the native primitive value type.",
              "type": "string",
              "enum": [
                "native"
              ]
            },
            "dataType": {
              "description": "Concrete primitive data type expected for the attribute value.",
              "type": "string",
              "enum": [
                "string",
                "boolean",
                "integer"
              ]
            }
          },
          "additionalProperties": false
        },
        {
          "description": "A value that references an entry defined in another section (e.g. a component or task name).",
          "type": "object",
          "required": [
            "kind",
            "section"
          ],
          "properties": {
            "kind": {
              "description": "Discriminator selecting the cross-section reference value type.",
              "type": "string",
              "enum": [
                "reference"
              ]
            },
            "section": {
              "description": "Name of the section whose entries this attribute value refers to (e.g. \"Components\", \"Tasks\").",
              "type": "string"
            }
          },
          "additionalProperties": false
        },
        {
          "description": "A value made up of one or more predefined flag keywords.",
          "type": "object",
          "required": [
            "kind",
            "flags"
          ],
          "properties": {
            "kind": {
              "description": "Discriminator selecting the flag-set value type.",
              "type": "string",
              "enum": [
                "flags"
              ]
            },
            "flags": {
              "description": "List of flag keywords that are valid for this attribute.",
              "type": "array",
              "items": {
                "$ref": "#/definitions/flagEntry"
              }
            }
          },
          "additionalProperties": false
        },
        {
          "description": "A path to a file. With \"mustExists\" it is validated to exist and to be a regular file; without it only invalid path characters are reported.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "description": "Discriminator selecting the file-path value type.",
              "type": "string",
              "enum": [
                "file"
              ]
            },
            "mustExists": {
              "description": "Present = the file must exist on the build machine at compile time (a build-machine source file). Absent = target/runtime path, only validated for invalid path characters.",
              "$ref": "#/definitions/mustExists"
            }
          },
          "additionalProperties": false
        },
        {
          "description": "A path to a directory. With \"mustExists\" it is validated to exist and to be a directory; without it only invalid path characters are reported.",
          "type": "object",
          "required": [
            "kind"
          ],
          "properties": {
            "kind": {
              "description": "Discriminator selecting the directory-path value type.",
              "type": "string",
              "enum": [
                "directory"
              ]
            },
            "mustExists": {
              "description": "Present = the directory must exist on the build machine at compile time (a build-machine source directory). Absent = target/runtime path, only validated for invalid path characters.",
              "$ref": "#/definitions/mustExists"
            }
          },
          "additionalProperties": false
        }
      ]
    },
    "flagEntry": {
      "description": "A single flag keyword that may be used in a flags-typed attribute.",
      "type": "object",
      "required": [
        "name",
        "description",
        "deprecated"
      ],
      "properties": {
        "name": {
          "description": "Flag keyword as written in the script (e.g. \"ignoreversion\", \"recursesubdirs\").",
          "type": "string"
        },
        "description": {
          "description": "Human-readable explanation of what the flag does, shown in documentation and tooltips.",
          "type": "string"
        },
        "deprecated": {
          "description": "File types in which this flag is deprecated (e.g. [\"iss\", \"isl\"]). Empty = not deprecated.",
          "$ref": "#/definitions/fileTargets"
        },
        "conflicts": {
          "description": "Other flags that cannot be combined with this flag, each with a type.",
          "type": "array",
          "items": {
            "description": "A single conflicting flag together with how severe the conflict is.",
            "type": "object",
            "required": [
              "flag",
              "type"
            ],
            "properties": {
              "flag": {
                "description": "Name of the conflicting flag keyword.",
                "type": "string"
              },
              "type": {
                "description": "Type of flag conflict. Depends on type an annotator mark this flag as superfluous because the declaring flag already implies it (or nullifies its effect).",
                "type": "string",
                "enum": [
                  "forbidden",
                  "ignored",
                  "redundant",
                  "requires"
                ]
              }
            },
            "additionalProperties": false
          }
        },
        "since": {
          "description": "Inno Setup version in which this flag was introduced (e.g. \"6.0.0\").",
          "type": "string"
        },
        "until": {
          "description": "Inno Setup version up to which this flag is supported; absent if still current.",
          "type": "string"
        }
      },
      "additionalProperties": false
    }
  }
}
